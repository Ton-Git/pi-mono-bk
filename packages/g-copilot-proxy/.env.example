# Server Configuration
PORT=8000
HOST=0.0.0.0

# Authentication Mode
# Options: managed (OAuth flow), passthrough (Bearer token)
AUTH_MODE=managed

# GitHub Enterprise URL (optional, for self-hosted GitHub)
# GITHUB_ENTERPRISE_URL=https://github.yourcompany.com

# CORS Configuration
# Comma-separated list of allowed origins, or * for all
CORS_ORIGINS=*

# Logging Level
# Options: debug, info, warn, error
LOG_LEVEL=info

# Data Directory
# Location for storing OAuth credentials and other data
DATA_DIR=./data

# pi-ai Cache Retention
# Options: default (5 min cache), long (24h cache for Anthropic, 1h for OpenAI)
PIAI_CACHE_RETENTION=default
